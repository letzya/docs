export const ButtonLeft = ({ href, color, content }) => {
  return (
    <div style={{ textAlign: "left", marginBottom: "1rem" }}>
      <a 
        href={href} 
        className={`button center button-${color}`}
      >
        {content}
      </a>
    </div>
  );
};
